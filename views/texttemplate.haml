%h2 テキストテンプレート
.tabbable.tabs-left
  = haml :_left_tab
  .tab-content
    #list.tab-pane.active
      %form{action:url("/text_template"), method:"post"}
        %input{name:"_method", type:"hidden", value:"delete"}
        %input.btn.btn-mini.btn-danger{type:"submit", value:"チェックしたテンプレートを削除"}
        %table.table.table-condensed
          %thead
            %tr
              %th
              %th id
              %th テンプレート名
              %th 作成日
              %th 更新日
          %tbody
            - @texttemplates.each do |texttemplate|
              %tr
                %td
                  %input{type:"checkbox", name:"id_collection[]", value:texttemplate.id}
                %td
                  = texttemplate.id
                %td
                  %a{href:url("/text_template/#{texttemplate.id}")}
                    = texttemplate.name
                %td
                  = texttemplate.created_at
                %td
                  = texttemplate.updated_at
    #new.tab-pane
      %form.form-horizontal{action:url("/text_template"), method:"post"}
        .control-group
          %label.control-label テンプレート名
          .controls
            %input.span8{type:"text", name:"name"}
        .control-group
          %label.control-label ヘッダー
          .controls
            %textarea.span8{rows:8, name:"header"}
            %span.help-block
              改行は&lt;br&gt;で表現してください。
        - (1..5).each do |i|
          .control-group
            %label.control-label タイトル#{i}
            .controls
              %input.span8{type:"text", name:"col#{i}_title", value:texttemplate_title(i)}
          .control-group
            %label.control-label テキスト#{i}
            .controls
              %textarea.span8{rows:8, name:"col#{i}_text"}
              %span.help-block
                改行は自動的に&lt;br&gt;に変換されます。
        .control-group
          %label.control-label フッター
          .controls
            %textarea.span8{rows:8, name:"footer"}
            %span.help-block
              改行は&lt;br&gt;で表現してください。
        .form-actions
          %input.btn.btn-primary{type:"submit"}
